// import { checkMissingFiles } from '../utils/fileChecker.js';

// checkMissingFiles()
//     .then(() => process.exit(0))
//     .catch((err) => {
//         console.error('File check failed:', err);
//         process.exit(1);
//     });

// // Automating It with Cron, Edit crontab, View or debug cron logs